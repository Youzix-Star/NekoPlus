# Copy to keystore.properties (git-ignored) to build a signed APK locally.
# On CI these values come from GitHub Actions secrets instead.
KEYSTORE_PATH=miao-release.jks
KEYSTORE_PASSWORD=your-store-password
KEY_ALIAS=miao
KEY_PASSWORD=your-key-password
